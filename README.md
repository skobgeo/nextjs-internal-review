# Lumen — стенд для интернал-ревью фронтенд-разработчика

Монорепозиторий с маркетинговым сайтом на **Next.js 16 (App Router)** и бэкендом на **Fastify + SQLite**.
Сайт работает, но в нём намеренно оставлены незаконченные места и дефекты — это и есть задания.

Формат: **три сессии по 60 минут**, лайвкодинг с ревьюером и обсуждение вслух.
У каждой сессии своя ветка: `session-1`, `session-2`, `session-3`. В каждой следующей ветке
задания предыдущей уже решены — вы всегда стартуете с чистого состояния, независимо от того,
что успели вчера.

В каждой сессии есть **ядро** (около 50 минут) и **резерв**. Ядро делаем обязательно,
резерв — если остаётся время. Не успеть резерв — нормально.

---

## Быстрый старт

Окружение поднимите **до** сессии и убедитесь, что сайт открывается. Задания до сессии
не смотрите — это договорённость.

```bash
git checkout session-1
mise install               # node 24.21.0, npm 12.0.2, pnpm 11.27.0
pnpm install
pnpm dev                   # поднимает оба приложения сразу
```

| Что | Где |
|---|---|
| Сайт | http://localhost:3000 (редиректит на `/en` или `/ru`) |
| API | http://localhost:3100 |
| **Swagger UI** | **http://localhost:3100/docs** ← контракт бэкенда, пригодится почти в каждом задании |

База SQLite создаётся и наполняется демо-контентом при первом запуске API.
Если нужно начать с чистого листа — `pnpm db:reset`. Подробности — в `docs/SETUP.md`.

### Команды

```bash
pnpm dev             # оба приложения (turbo)
pnpm test            # все тесты один раз
pnpm test:core       # vitest в watch-режиме для @repo/core     ← сессия 1
pnpm test:contracts  # vitest в watch-режиме для @repo/contracts ← сессия 1
pnpm typecheck       # tsc --noEmit по всему монорепозиторию
pnpm lint            # eslint
pnpm build           # прод-сборка
pnpm tasks           # список маркеров заданий с файлами и строками
pnpm tasks S2-03     # то же, но по одному заданию
```

**Состояние на старте любой ветки:** `pnpm typecheck`, `pnpm lint` и `pnpm build` проходят.
В ветке `session-1` `pnpm test` красный: падает **47 тестов**, и это ровно задания первой сессии.

---

## Карта репозитория

```
apps/
  web/        Next.js 16, App Router, порт 3000 — здесь большая часть заданий
  api/        Fastify 5 + Drizzle + SQLite + Swagger, порт 3100 — трогать не нужно
packages/
  core/       чистый TypeScript: алгоритмы, growth-логика, типы i18n (+ тесты)
  contracts/  zod-схемы и типобезопасный HTTP-клиент, общие для фронта и бэка
  ui/         собственный ui-kit по атомарному дизайну, CSS Modules
  eslint-config/, typescript-config/
```

Структура `packages/ui` — атомарный дизайн:

```
atoms/       Button, Input, Textarea, Select, Checkbox, Text, Icon, VisuallyHidden, Container, Stack
molecules/   FormField, ArticleCard, Breadcrumbs, Pagination
organisms/   SiteHeader, Hero, MetricsBand, FeatureGrid, ArticleGrid, CtaBand, SiteFooter
templates/   PageShell
tokens/      дизайн-токены (CSS custom properties) и reset
```

Правило ui-kit: компоненты не содержат сырых значений цвета и размера — только ссылки на токены
из `packages/ui/src/tokens/tokens.css`.

### Что уже сделано за вас

- бэкенд готов целиком, со Swagger и i18n по заголовкам `Content-Language` / `Vary`;
- дизайн-токены, большая часть ui-kit, разметка страниц;
- маршрутизация локалей (`proxy.ts`), загрузка словарей с бэкенда;
- тесты ко всем заданиям первой сессии.

### Как это проверяется

- **Сессия 1** — `pnpm test:core` / `pnpm test:contracts` в watch-режиме: зелёный тест = задание сдано.
  Типы проверяются глазами в редакторе (см. `packages/core/src/i18n/type-playground.ts`).
- **Сессии 2 и 3** — браузер: DevTools, режим устройства (320 / 768 / 1440 px), вкладка Network,
  «Просмотр кода страницы», Lighthouse. Плюс обсуждение вслух.

---

## Сессия 1 — JavaScript, алгоритмы, TypeScript, валидация

Ветка `session-1`. Работаем в `packages/core` и `packages/contracts`. Держите открытым `pnpm test:core`.

### Ядро (≈ 50 минут)

| # | ID | Тема | Файл | Что сделать | ~мин |
|---|---|---|---|---|---|
| 1 | **S1-01** | Коллекции и сложность | `packages/core/src/collections/group-by.ts` | Реализовать `groupBy` и `indexBy` (один проход, любой iterable). Рядом — `buildArticleIndex`: результат правильный, но на реальных объёмах функция складывается. Найти причину и переписать. | 10 |
| 2 | **S1-03** | Замыкания, `this`, таймеры | `packages/core/src/timing/debounce.ts` | В реализации **три дефекта**. Тесты показывают симптомы — найти причины и починить, не меняя публичный API. | 10 |
| 3 | **S1-02** | Асинхронность | `packages/core/src/async/map-with-concurrency.ts` | Написать map с ограничением параллелизма: порядок результатов по входу, не больше `limit` одновременных задач, корректное поведение при ошибке и при некорректном лимите. | 15 |
| 4 | **S1-07** | Валидация ответов | `packages/contracts/src/http/safe-fetch.ts` | Довести `safeFetch` до состояния, когда он не бросает исключений и возвращает `Result` с честным `kind` ошибки, а успех — это **провалидированные** данные. Проверка: `pnpm test:contracts`. | 15 |

### Резерв

| ID | Тема | Файл | Что сделать | ~мин |
|---|---|---|---|---|
| S1-06 | Дженерики | `packages/core/src/i18n/translation-key.ts` | Сначала рантайм: дописать `createTranslator` — интерполяция параметров и фолбэк на словарь дефолтной локали. Потом тип: сделать `TranslationKey<TDict>` объединением точечных путей до строковых листьев словаря (проверяется в `type-playground.ts`). | 15 |
| S1-07b | Схемы блога | `apps/web/src/lib/api/schemas.ts`, `apps/web/src/lib/api/articles.ts` | Откройте http://localhost:3000/ru/blog и сравните карточки с ответом `GET /api/articles` в Swagger. Ошибок в консоли нет, и тем не менее страница показывает неправду. Сколько расхождений с контрактом вы найдёте? Описать схемы ответов и перевести `articles.ts` на `apiGet` + схемы. | 15 |
| S1-04 | Структуры данных | `packages/core/src/cache/lru-cache.ts` | LRU-кэш с `get`/`set` за O(1), вытеснением самого давнего ключа и предсказуемым порядком `keys()`. | 12 |
| S1-05 | Деревья | `packages/core/src/tree/build-nav-tree.ts` | Собрать дерево навигации из плоского списка и найти путь до узла. Результат виден на сайте: хлебные крошки на странице статьи сейчас пустые. | 15 |
| S1-B1 | Бонус: атрибуция | `packages/core/src/growth/utm.ts` | `resolveAttribution`: first-touch не перезаписывается, прямой заход не затирает платный источник. | 10 |
| S1-B2 | Бонус: скоринг заявки | `packages/core/src/growth/lead-score.ts` | Посчитать скор заявки по описанным правилам. Результат виден в поле `score` ответа `POST /api/leads`. | 10 |

### Где искать дефекты

| Зона | Сколько |
|---|---|
| `packages/core/src/timing/debounce.ts` | 3 |
| Расхождения с контрактом API в блоге (`web/src/lib/api/articles.ts`) | 5 |

---

---

---

## Правила

- Документация, Swagger, MDN, исходники в `node_modules` — можно и нужно.
- Поисковик — можно.
- AI-ассистенты (Copilot, Cursor, ChatGPT, Claude) — нет.
- Не успеть резерв — нормально. Гораздо важнее, как вы рассуждаете вслух: почему именно так,
  что бы вы сделали иначе, будь это прод.
- Если задание кажется странным или спорным — скажите об этом. Аргументированное несогласие
  с формулировкой засчитывается в плюс.

## Заметки о стеке

- **Next.js 16**: Turbopack по умолчанию, `params` и `searchParams` — промисы, `middleware.ts`
  переименован в `proxy.ts`, `revalidateTag` принимает второй аргумент, `next lint` удалён
  (ESLint запускается напрямую).
- **Бэкенд** отдаёт `snake_case`, даты — строками ISO 8601 в UTC, списки — конвертом
  `{ items, total, page, per_page }`. Локаль выбирается через `?locale=`, затем `Accept-Language`,
  затем дефолтная `en`; фактический язык ответа всегда в заголовке `Content-Language`.
- **Частичный перевод** — это не баг: две статьи из шести не переведены на русский,
  и бэкенд честно сообщает об этом в `content_locale`.
