/**
 * [S1-02] Ограниченный по параллелизму map.
 *
 * Типичная задача growth-сайта: прогреть 200 URL, сходить в CMS за 50 статьями,
 * дёрнуть внешний API — но не положить его пачкой из 200 одновременных запросов.
 *
 * Требования:
 *  - в любой момент времени выполняется не больше `limit` задач;
 *  - результат соответствует порядку входного массива, а не порядку завершения;
 *  - `limit >= items.length` ведёт себя как `Promise.all`;
 *  - пустой вход — пустой результат, worker не вызывается;
 *  - `limit < 1` (в том числе 0 и NaN) — промис реджектится `RangeError`;
 *  - при отказе одной задачи промис реджектится первой ошибкой, и новые задачи
 *    из очереди больше НЕ стартуют (уже запущенные доигрывают).
 */
export async function mapWithConcurrency<T, R>(
  _items: readonly T[],
  _limit: number,
  _worker: (item: T, index: number) => Promise<R>,
): Promise<R[]> {
  return [];
}
